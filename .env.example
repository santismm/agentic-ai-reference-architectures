# Copy to `.env` and fill in only if you swap EchoModel for a real backend.
# The core package and all examples/tests run WITHOUT any of these.

# --- Vertex AI (optional: pip install '.[vertex]') ---
# GOOGLE_CLOUD_PROJECT=your-gcp-project-id
# GOOGLE_CLOUD_LOCATION=us-central1
# Authenticate with: gcloud auth application-default login

# --- Generic (if you wire a different provider) ---
# MODEL_NAME=gemini-2.5-pro
